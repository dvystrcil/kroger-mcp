name: validate
on: [pull_request]
jobs:
  manifests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: dvystrcil/kustomize-validate-action@feat/reloader-coverage-check
