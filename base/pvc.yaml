apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kroger-mcp-shadow-state
  namespace: kroger-mcp
  labels:
    app: kroger-mcp
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
