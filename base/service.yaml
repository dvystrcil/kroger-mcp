apiVersion: v1
kind: Service
metadata:
  name: kroger-mcp
  namespace: kroger-mcp
  labels:
    app: kroger-mcp
spec:
  type: ClusterIP
  selector:
    app: kroger-mcp
  ports:
  - name: mcp
    port: 8765
    targetPort: mcp
    protocol: TCP
